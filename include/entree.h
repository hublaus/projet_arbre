#include "ligne_de_commande_arbre_couvrant.h"
#include "emplacement.h"

/**
* @brief lit le fichier binaire et stocke les emplacements dans un tableau à l'adresse pointée par tableau_emplacement
* @param opt pointeur sur la structure options contenant les options de l'utilisateur
* @return tableau_emplacement tableau contenant les emplacements
*/

emplacement* lecture_fichier_binaire(options *opt);